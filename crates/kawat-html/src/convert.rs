//! Tag conversion: normalize HTML tags to internal tag set.
//! h1-h6 → head, b/strong/em/i → hi, a → ref, ul/ol → list, br → lb, etc.
pub fn convert_tags(_html: &str) -> String {
    todo!("convert_tags")
}
